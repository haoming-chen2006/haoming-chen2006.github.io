node_modules

# Generated by `npm run build:data` from the FreeKill checkout. Regenerate with
# `npm run build`; the published copies under ../freekill/ are the deployment.
public/lua-bundle.json
public/lua-manifest.json
public/asset-manifest.json
public/overview.json
public/assets/

# public/fonts/ IS committed: building it needs uv, fonttools and a download,
# and the OFL license file must travel with the face.

# Local overrides. The publishable key has an inline fallback anyway.
.env
.env.local
